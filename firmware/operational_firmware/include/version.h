#pragma once

// Open Field Logger Lab firmware version.
//
// Bump this whenever you build a new release. It's shown to students in
// the Configuration tab's Firmware Update card so they can confirm which
// version is currently running on their board — handy both for keeping a
// classroom set of ~20 units in sync and for confirming an OTA upload
// actually took effect after the reboot.
#define OFL_FIRMWARE_VERSION "1.0"
